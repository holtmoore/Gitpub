<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= drink.name %></title>
    <style type="text/css">
        body {
          color: blanchedalmond;
          background-color: steelblue;
        }
    </style>
</head>
<body>
    <h1>At Foo Bar</h1>
    <h2><%= drink.name.charAt(0).toUpperCase() + drink.name.slice(1) %></h2>
    <img src="<%= drink.image %>.png" alt="<%= drink.name %>">
    <h3>Price: <%= drink.price %></h3>
    <a href="/drinks">Back</a>
</body>
</html>
